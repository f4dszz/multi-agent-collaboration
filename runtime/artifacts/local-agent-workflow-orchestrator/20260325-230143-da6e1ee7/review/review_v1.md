Now I have full context — the plan, the repo README, the system architecture doc, the workflow JSON, the product architecture, and the role definitions. Let me produce the strict review.

---

Decision: **revise**
Risk-Tags: `scope-drift`, `repo-conflict`, `missing-role`, `wrong-storage-model`, `premature-adapter-work`, `weak-ac`, `unresolved-blockers`

---

Blockers:
- B1 | Plan ignores existing repo architecture entirely | The repo already has a defined SQLite + file hybrid storage architecture (`SYSTEM_ARCHITECTURE.md`), a 10-state state machine, `backend/app/{api,domain,services}` skeleton, `tests/`, `roles/`, and `workflows/default_local_workflow.json`. The plan proposes a wholly parallel structure (`runs/<run_id>/timeline.jsonl`) that directly conflicts with the existing artifact layout (`artifacts/{project_slug}/{run_id}/plan/review/implementation/...`). Executing this plan as-written would create two incompatible storage systems.
- B2 | Human gate is scoped out but is a core repo requirement | The plan explicitly excludes "Human-in-the-loop approvals" from v1 scope. However, `default_local_workflow.json` defines **two mandatory human gates** (`plan_gate` at `awaiting_plan_approval`, `final_gate` at `awaiting_final_approval`). `PRODUCT_ARCHITECTURE.md` lists "用户批准后开始实施" and final approval as non-negotiable MVP steps. Removing human gates contradicts the repo's fundamental correctness guarantee.
- B3 | Verifier/Judge role is completely absent | The plan only models `executor + reviewer`. The repo defines a conditional third role (`verifier`) with explicit trigger rules: revision threshold ≥ 2, risk tags hitting `database/security/concurrency/architecture`, Reviewer-Executor deadlock, or auto-verification failure. This logic is already encoded in `default_local_workflow.json` (`verifier_trigger`). A plan that omits this role cannot integrate with the existing workflow engine.
- B4 | Plan is phase-2 work attempted before phase-1 is confirmed stable | `SYSTEM_ARCHITECTURE.md` Section 9 explicitly stages the work: **Phase 1** = stabilize state machine + artifact store + SQLite repo (with mock adapters); **Phase 2** = connect real CLI adapters + WebSocket push + Git diff. The README repeats: "下一阶段才应该继续接入真实的 CLI adapter". The plan jumps directly to Phase 2 (real CLI adapters) without confirming Phase 1 deliverables are solid, creating an unstable foundation.
- B5 | Provider interface signatures conflict with repo-defined adapter contract | The plan proposes `prepare() / run_step(step_spec) / collect_artifacts() / normalize_result()`. The system architecture doc defines `prepare_context(role, stage, project_path, artifacts) / launch_task(command, timeout, env) / capture_output() / normalize_result()`. These are different method names and parameter shapes. Implementing the plan's interface would fork the adapter design and break integration with the orchestrator layer already specified.
- B6 | Six open questions are already answered in existing docs but treated as unresolved | The plan lists as open: storage format choice, artifact definition, review semantics, timeline format, reviewer-executor provider assignment, and existing repo constraints. All six are answered in `SYSTEM_ARCHITECTURE.md`, `PRODUCT_ARCHITECTURE.md`, `roles/reviewer.md`, and `default_local_workflow.json`. Building on the wrong answers (e.g., file-only storage instead of SQLite hybrid) cascades into architectural debt.

---

Concerns:
- C1 | Finding system (blocker/concern/suggestion) is entirely missing | `roles/reviewer.md` and `PRODUCT_ARCHITECTURE.md` define that every reviewer output must produce structured `findings` with severity levels (`blocker / concern / suggestion`), each must be locatable, actionable, and verifiable, and each must be tracked to closure. The plan has no finding schema, no finding persistence, no finding-to-response linkage, and no criterion for finding resolution. This is not a cosmetic gap — it is the core data model of the review workflow.
- C2 | Single-pass review assumption contradicts multi-round state machine | The plan states "executor runs once, reviewer runs once, no automatic rework loop". But the existing state machine has `plan_revision` as an explicit state and the standard flow shows `plan_review → plan_revision → plan_review` iteration. The verifier trigger threshold of 2 revision rounds makes no sense if revisions are not modeled.
- C3 | Artifact directory layout conflicts with existing structure | Plan proposes `runs/<run_id>/steps/<step_id>.json` and `runs/<run_id>/timeline.jsonl`. Repo defines `artifacts/{project_slug}/{run_id}/plan/`, `/review/`, `/response/`, `/implementation/`, `/verification/`, `/timeline/`. These are structurally incompatible and using both would create two competing artifact roots.
- C4 | Timeline event set is underspecified relative to 10-state machine | The plan defines 6 timeline events (`run_started`, `step_started`, `step_completed`, `artifact_collected`, `run_failed`, `run_completed`). The repo's state machine has 10 states including `drafting_plan`, `plan_review`, `plan_revision`, `awaiting_plan_approval`, `implementing`, `implementation_review`, `verifying`, `awaiting_final_approval`, `completed`, `blocked`. Each state transition should be a timeline event. The plan's 6-event model cannot reconstruct the full workflow history.
- C5 | No integration path with existing tests | The repo already has `tests/test_artifact_store.py`, `tests/test_repository.py`, and `tests/test_state_machine.py`. The plan's Step 10 proposes creating new fixture-based tests without any mention of running or extending existing tests. This risks regression in already-validated behavior.
- C6 | Secret redaction is acknowledged as a risk but has no implementation plan | The plan notes secret leakage as a risk (prompts stored verbatim), and Step 8 mentions "redacting secrets." No redaction mechanism, redaction scope, or test coverage for redaction is defined. This is a security control that cannot be left as a vague note.
- C7 | Non-interactive invocation of Claude Code CLI is not validated as feasible | Claude Code CLI (`claude`) is architecturally interactive. The plan assumes it can be driven non-interactively via subprocess as a black-box. This assumption has not been validated. If Claude Code CLI requires a TTY or interactive session, the entire adapter model collapses without an alternative (e.g., SDK-based invocation instead).

---

Suggestions:
- S1 | Rebase the plan onto existing repo structure explicitly | The plan should open with an inventory of what exists (state machine, SQLite schema, artifact store, mock adapter interface) and define what must be added or modified — not propose a greenfield structure that ignores them. Each implementation step should reference the existing module it extends.
- S2 | Resolve all six open questions before sequencing implementation steps | Questions about storage format, artifact definition, review semantics, and timeline format are pre-conditions for every implementation step. They should be resolved in Step 1, sourced from existing docs, not deferred to future decisions.
- S3 | Add finding schema to the run contract in Step 1 | The canonical workflow schema must include `findings: [{id, severity, description, state, response}]` alongside the current fields. This is the linchpin of the review flow and must be defined before the adapter or orchestrator is built.
- S4 | Insert explicit phase-1 gate before CLI adapter work | Add a checkpoint: confirm that `test_state_machine.py`, `test_artifact_store.py`, and `test_repository.py` all pass with the current implementation before writing any real CLI adapter. If they fail or are incomplete, that is the actual first task.
- S5 | Specify acceptance criteria with measurable thresholds | Replace vague criteria like "stable artifact directory" and "provider failures are surfaced" with concrete, testable statements: exact directory structure, required JSON schema for step results, required fields in `timeline_events` table, required finding fields, timeout enforcement verified by test, and secret strings absent from all persisted files.
- S6 | Validate Claude Code CLI and Codex CLI non-interactive invocation before committing to adapter design | Add a spike/proof-of-concept step: invoke each CLI with a minimal prompt in non-interactive mode, capture exit code and stdout, confirm output is parseable. Gate adapter implementation on this result. If either CLI requires interactive mode, the plan must pivot to SDK-based invocation.
- S7 | Align provider adapter interface with repo-defined contract | Adopt `prepare_context(role, stage, project_path, artifacts)` / `launch_task(command, timeout, env)` / `capture_output()` / `normalize_result()` as the canonical interface. Do not introduce a parallel interface definition.
- S8 | Explicitly model revision rounds and finding closure in acceptance criteria | Add criteria: "After a reviewer step emits a blocker finding, the orchestrator must not advance to `implementing` until that finding's state is `resolved`." And: "Revision count is persisted and increments correctly across `plan_review → plan_revision → plan_review` cycles."

---

Summary:
- The plan is well-structured in isolation but is architecturally disconnected from the existing repository it is meant to extend. It omits three critical role/workflow elements (Verifier, Human Gate, Finding tracking), proposes a storage model (file-only) that contradicts the established SQLite hybrid design, uses a different artifact directory layout than the repo, and defines a provider interface that conflicts with the one already specified in `SYSTEM_ARCHITECTURE.md`. It also attempts Phase 2 work (real CLI adapters) before verifying that Phase 1 foundations (state machine, repository, artifact store) are stable, which the repo's own README explicitly warns against. Six "open questions" are already answered in existing documentation but are treated as unresolved, risking decisions being made in contradiction to established design. The acceptance criteria are too vague to be enforceable. The plan requires a substantive revision — not cosmetic edits — before execution can proceed safely.
请在这台新的 Windows 电脑上，帮我复现旧电脑上的代理与 Claude 使用环境。不要只给建议，请直接检查、配置、验证，并在完成后告诉我哪些是真的已经生效了。

目标状态：

1. 使用 Clash Verge Rev 作为唯一的本地代理内核
2. 使用我的 RioLU 订阅作为默认出口
3. 接入我的 IPRoyal 静态代理作为额外出口
4. 不要使用整机 Global 把所有流量都压到 IPRoyal
5. 最终采用 Rule 模式：
   - 普通流量继续走 RioLU 默认节点
   - Claude / Anthropic 相关流量尽量走 IPRoyal Exit
6. TUN 保持关闭
7. System Proxy 开启
8. Clash 本地 mixed-port 使用 7897
9. Claude Code CLI 通过官方 HTTP_PROXY / HTTPS_PROXY 方式走本地 Clash 口 `http://127.0.0.1:7897`
10. 最后做验证，不要只看配置文件

重要背景：

- 旧电脑上已经证明：
  - Claude Code CLI 访问 `api.anthropic.com` 可以走 IPRoyal
  - 但 `claude.ai` 网页对当前这枚 IPRoyal 静态 IP 可能会返回 Cloudflare challenge / 403
- 所以不要把“网页一定能用静态 IP 登录”当成必须成功项
- 重点是把环境搭成和旧电脑一致，并把实际路由验证清楚
- 如果网页走 IPRoyal 仍然 challenge，请明确告诉我这是 IP 风控问题，不是配置问题

你要完成的事情：

A. 检查并安装/确认以下组件
- Clash Verge Rev
- Claude Code CLI
- Chrome（如果需要专用浏览器测试）

B. 配置 Clash Verge Rev
- 导入 RioLU 订阅
- 添加 IPRoyal 静态代理节点
- 建立 `IPRoyal Exit` 组
- 如果需要链式代理：
  - 第一跳用 RioLU 节点
  - 第二跳用 IPRoyal 静态代理
- 最终模式使用 `Rule`
- `System Proxy` 开
- `TUN` 关
- `mixed-port` 为 `7897`

C. 为 Claude 相关流量添加规则
请尽量完整覆盖 Claude / Anthropic 相关内容，至少包括这些：
- `claude.ai`
- `a.claude.ai`
- `downloads.claude.ai`
- `api.anthropic.com`
- `a-api.anthropic.com`
- `assets-proxy.anthropic.com`
- `cdn.anthropic.com`
- `download.anthropic.com`
- `a-cdn.anthropic.com`
- `s-cdn.anthropic.com`
- `anthropic.skilljar.com`
- `api-iam.intercom.io`
- `widget.intercom.io`
- `nexus-websocket-a.intercom.io`
- `browser-intake-us5.datadoghq.com`
- `http-intake.logs.us5.datadoghq.com`

目标是：
- Claude / Anthropic 核心流量优先走 `IPRoyal Exit`
- 但不要把整机其它无关流量全都强行送去 IPRoyal

D. 配置 Claude Code CLI
请直接配置用户目录下的：
- `C:\Users\<当前用户名>\.claude\settings.json`

把官方代理环境写进去：
```json
{
  "env": {
    "HTTP_PROXY": "http://127.0.0.1:7897",
    "HTTPS_PROXY": "http://127.0.0.1:7897",
    "NO_PROXY": ""
  }
}
```

如果文件里已有其它设置，请保留原有结构，只补上 `env` 配置，不要破坏别的字段。

E. 验证
请不要只说“应该可以”，而是实际验证：

1. 验证本地 Clash 口存在
2. 验证 `api.anthropic.com` 的请求是否命中 `IPRoyal Exit`
3. 验证普通网站是否仍走 RioLU 默认节点
4. 验证 Claude Code CLI 命令是否存在
5. 运行 `claude auth status` 确认 CLI 是否可用
6. 如果需要，使用 `curl` 和 Clash 日志双重验证

建议使用的验证方式：
- `curl.exe --proxy http://127.0.0.1:7897 https://api.anthropic.com`
- 查看 Clash Verge sidecar / service 日志，确认 `api.anthropic.com` 是否走 `IPRoyal Exit`
- 同时测试一个普通网站，确认它不是也被一起送去 IPRoyal

F. 输出结果时请明确告诉我：
- 当前是否是 `Rule`
- `System Proxy` 是否开启
- `TUN` 是否关闭
- `mixed-port` 是否为 `7897`
- Claude Code CLI 是否还在
- `api.anthropic.com` 是否实际走到了 IPRoyal
- `claude.ai` 网页是否可用
- 如果网页不可用，是不是 Cloudflare challenge / 403 / timeout
- 哪些文件被改了，给出完整路径

约束：
- 不要默认切整机 Global 作为最终方案
- 不要默认开启 TUN
- 不要删除我现有无关配置
- 如果遇到失败，请先查运行态和日志，不要只猜
- 如果某一步需要管理员权限，请明确执行并告诉我结果
